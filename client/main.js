


/* import { 
  getNode, loadStorage, saveStorage, deleteStorage
} from "./lib/index.js";

const textField = getNode('#textField')
const deleteButton = getNode('input[value="삭제"]')

loadStorage('area').then((res)=>{
  textField.value = res;
})

function inputHandler(){
  saveStorage('area', textField.value)
}

function inputDeleteHandler(){
  deleteStorage('area')
}

textField.addEventListener('input', inputHandler)
deleteButton.addEventListener('click', inputDeleteHandler) */

let n=1;

function solution(n) {
  let answer = [];
  answer.map((acc, i) => i+1)
}

solution();